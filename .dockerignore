# Build artifacts
gonzbd
*.exe

# UI build artifacts (rebuilt in Docker)
ui/node_modules/
ui/dist/
ui/.svelte-kit/

# Test fixtures and data
test/
*.test

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# Git (keep .git/ for commit/date stamping in the build)
.gitignore

# Docker
docker-compose*.yml
Dockerfile

# Docs (not needed in the image)
docs/
*.md
!README.md

# Config/state (user data, not part of build)
admin/
downloads/
config/
