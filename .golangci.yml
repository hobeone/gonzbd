version: "2"

run:
  timeout: 5m
  tests: true
  build-tags:
    - integration
    - uitest
    - crash

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - unconvert
    - unparam
    - bodyclose
    - copyloopvar
    - errorlint
    - gocritic
    - gosec
    - nilerr
    - prealloc
    - revive
    - wastedassign
    - modernize
    - ireturn
    - intrange
    - iface
    - gocritic
    - sloglint
  settings:
    errcheck:
      check-type-assertions: true
      check-blank: false
    gocritic:
      enabled-tags:
        - diagnostic
        - performance
        - style
      disabled-checks:
        - hugeParam
        - rangeValCopy
    revive:
      rules:
        - name: exported
          disabled: false
        - name: package-comments
          disabled: false
        - name: var-naming
        - name: unused-parameter
          disabled: true
    gosec:
      excludes:
        - G104  # handled by errcheck
  exclusions:
    paths:
      - node_modules
    rules:
      - path: _test\.go
        linters:
          - gosec
          - errcheck
          - gocritic
          - unparam

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/hobeone/gonzbd

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
